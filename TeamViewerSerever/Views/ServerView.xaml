<Window x:Class="TeamViewerSerever.Views.ServerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TeamViewerSerever.Views"
        mc:Ignorable="d"
        Icon="Photo/HomeServer.ico"
        Title="ServerView" FontSize="20" Height="200" Width="400">
    <StackPanel>
        <Button Command="{Binding StartCommand}">Start</Button>
        <DockPanel>
            <Label Width="100">Ip</Label>
            <TextBox Text="{Binding Ip}"></TextBox>
        </DockPanel>
        <DockPanel>
            <Label Width="100">Port</Label>
            <TextBox Text="{Binding Port}"></TextBox>
        </DockPanel>
        <Label>Info for connected...</Label>
    </StackPanel>
</Window>
